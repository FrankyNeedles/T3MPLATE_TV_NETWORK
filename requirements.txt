# T3MPLATE TV WORLD - minimal honest MVP runtime
# Clean deps only: world (SQLAlchemy), render (Pillow+numpy), decision (pydantic).
# ffmpeg is external (on PATH); NO pygame, NO sounddevice, NO window.
sqlalchemy>=2.0
Pillow>=10.0
numpy>=1.24
pydantic>=2.0

# dev / test
pytest>=8.0
